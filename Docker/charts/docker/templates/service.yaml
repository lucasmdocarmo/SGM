apiVersion: v1
kind: Service
metadata:
  name: {{ template "docker.fullname" . }}
  labels:
    app: {{ template "docker.name" . }}
    chart: {{ template "docker.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "docker.name" . }}
    release: {{ .Release.Name }}
